import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Check, Flame, LockKeyhole, MessageCircle, Target, Trophy } from "lucide-react";
import { useState } from "react";
import { MemberShell } from "@/components/brand/member-shell";
import { member, mission, progress, activities, upcomingSession, accountability } from "@/data/member";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Progress } from "@/components/ui/progress";
import { toast } from "sonner";
export const Route = createFileRoute("/dashboard")({ component: DashboardPage });
function DashboardPage() { const [done, setDone] = useState(false); return <MemberShell><div className="mx-auto max-w-7xl px-4 py-8 sm:px-8 lg:py-12"><div className="flex flex-col gap-8"><header className="flex flex-col gap-4 border-b border-border pb-8"><p className="eyebrow text-gold">The Den · Member dashboard</p><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><h1 className="display-xl text-balance">Good evening, {member.name}.</h1><p className="mt-3 max-w-xl text-base leading-relaxed text-muted-foreground">The work continues when nobody is watching.</p></div><Badge variant="outline" className="w-fit border-gold/40 text-gold">{member.level}</Badge></div></header><section className="grid gap-4 lg:grid-cols-[1.4fr_0.8fr]"><Card variant="gold" className="overflow-hidden"><CardHeader><div className="flex items-center justify-between"><div><p className="eyebrow text-gold">Current mission</p><CardTitle className="mt-2 text-xl">Your 1% Mission</CardTitle></div><Target className="size-5 text-gold" /></div><CardDescription className="max-w-xl text-base">{mission.title}. {mission.detail}</CardDescription></CardHeader><CardContent><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><p className="text-sm text-muted-foreground">Due {mission.due}</p><Button variant={done ? "secondary" : "gold"} onClick={() => { setDone(!done); toast(done ? "Mission reopened" : "Mission marked complete"); }}>{done ? <><Check data-icon="inline-start" />Completed</> : <>Mark complete <ArrowRight data-icon="inline-end" /></>}</Button></div></CardContent></Card><Card variant="elevated"><CardHeader><CardTitle className="flex items-center justify-between">Consistency <Flame className="size-5 text-gold" /></CardTitle><CardDescription>Your proof is in the pattern.</CardDescription></CardHeader><CardContent><div className="flex items-end gap-2"><span className="display-stat text-gold">{member.streak}</span><span className="pb-2 text-sm text-muted-foreground">day streak</span></div><Progress value={member.consistency} className="mt-4" /><div className="mt-3 flex justify-between text-xs text-muted-foreground"><span>{member.consistency}% this week</span><span>4 missions</span></div></CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card variant="bordered"><CardHeader><CardTitle>Learning progress</CardTitle></CardHeader><CardContent><p className="display-stat">{progress.percentage}%</p><p className="mt-2 text-sm text-muted-foreground">{progress.completedLessons} of {progress.totalLessons} lessons completed</p><Link to="/mindset"><Button variant="outline" size="sm" className="mt-5">Continue learning <ArrowRight /></Button></Link></CardContent></Card><Card variant="bordered"><CardHeader><CardTitle>Accountability check-in</CardTitle></CardHeader><CardContent><MessageCircle className="size-5 text-gold" /><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{accountability.prompt}</p><p className="mt-4 text-xs text-muted-foreground">{accountability.replies} pack members checked in</p></CardContent></Card><Card variant="bordered"><CardHeader><CardTitle>Next Wolf Session</CardTitle></CardHeader><CardContent><p className="font-semibold">{upcomingSession.title}</p><p className="mt-2 text-sm text-muted-foreground">{upcomingSession.date} · {upcomingSession.time}</p><Badge variant="secondary" className="mt-4">{upcomingSession.host}</Badge></CardContent></Card></section><section className="grid gap-4 lg:grid-cols-[1fr_1fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><Trophy className="size-4 text-gold" /> Recent Pack activity</CardTitle></CardHeader><CardContent><div className="flex flex-col gap-4">{activities.map((item) => <div key={item.label} className="flex items-start justify-between gap-4 border-b border-border pb-4 last:border-0 last:pb-0"><div><p className="text-sm font-semibold">{item.label}</p><p className="mt-1 text-sm text-muted-foreground">{item.detail}</p></div><span className="shrink-0 text-xs text-muted-foreground">{item.time}</span></div>)}</div></CardContent></Card><Card variant="crimson"><CardHeader><CardTitle>One mark at a time.</CardTitle><CardDescription>Small actions compound into a different life. Keep your word today.</CardDescription></CardHeader><CardContent><LockKeyhole className="size-5 text-crimson-tint" /></CardContent></Card></section></div></div></MemberShell>; }
