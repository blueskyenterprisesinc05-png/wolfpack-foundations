import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowRight, Check } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Logo } from "@/components/brand/logo";
export const Route = createFileRoute("/onboarding")({ component: OnboardingPage });
function OnboardingPage() { const navigate = useNavigate(); return <main className="grain flex min-h-screen items-center justify-center bg-background px-5 py-10"><div className="w-full max-w-2xl"><Logo size="sm" /><p className="eyebrow mt-16 text-gold">You are on the list</p><h1 className="display-xl mt-4 max-w-lg">The first step is showing up.</h1><p className="body-lg mt-5 max-w-lg text-muted-foreground">We will let you know when your invitation is ready. In the meantime, choose the track you want to make your own.</p><div className="mt-10 grid gap-3 sm:grid-cols-3">{["Build wealth", "Sharpen focus", "Lead better"].map((track) => <button key={track} className="flex min-h-28 flex-col justify-between rounded-xl border border-border bg-charcoal p-4 text-left transition-colors hover:border-gold"><span className="flex h-7 w-7 items-center justify-center rounded-full bg-forest/20 text-forest"><Check className="h-4 w-4" /></span><span className="font-semibold">{track}</span></button>)}</div><Button className="mt-8" size="lg" onClick={() => navigate({ to: "/dashboard" })}>Preview The Den <ArrowRight /></Button><p className="mt-5 text-sm text-muted-foreground"><Link to="/" className="hover:text-foreground">Return home</Link></p></div></main>; }
declare module "@tanstack/react-router" { interface FileRoutesByPath { "/onboarding": { parentRoute: typeof import("./__root").Route } } }
