/**
 * @deprecated MemberShell has been replaced by AppShell.
 *
 * AppShell is the single member layout owner as of Phase 1 of the UI redesign.
 * This file is kept only as a re-export to prevent any stale import from
 * causing a build error. New code must import AppShell directly.
 *
 * @see src/components/brand/app-shell.tsx
 */
export { AppShell as MemberShell } from "@/components/brand/app-shell";
